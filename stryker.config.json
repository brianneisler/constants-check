{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "'mutate' covers the full analysis library. The CLI entry (cli.ts), the barrel export (index.ts), and type-only files carry no testable logic and are excluded. 'break' is the minimum mutation score that fails the run; the suite currently scores ~85%, so 80 locks in progress with headroom for the known equivalent mutants in deepEqual/fileUtils. Raise it as coverage improves.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "checkers": ["typescript"],
  "tsconfigFile": "tsconfig.json",
  "coverageAnalysis": "perTest",
  "mutate": ["src/**/*.ts", "!src/cli.ts", "!src/index.ts", "!src/types/**", "!src/**/*.d.ts"],
  "ignorePatterns": ["dist", "coverage", ".stryker-tmp"],
  "incremental": true,
  "incrementalFile": ".stryker-tmp/incremental.json",
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "thresholds": { "high": 90, "low": 80, "break": 80 }
}
