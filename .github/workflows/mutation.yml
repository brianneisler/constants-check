name: Mutation Testing

# Mutation testing is slower than unit tests, so it runs as a separate job on a
# single Node version rather than inside the main CI matrix. It enforces a
# minimum mutation score (stryker.config.json sets break: 80) and uploads the
# HTML report as an artifact. Raise the break threshold as coverage improves.

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/**'
      - '__tests__/**'
      - 'stryker.config.json'
      - '.github/workflows/mutation.yml'
  workflow_dispatch:

jobs:
  mutation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Stryker's --since/incremental support benefits from full history.
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run mutation tests
        run: npm run test:mutation

      - name: Upload mutation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mutation-report
          path: reports/mutation
          if-no-files-found: ignore
